resources:
  /:
    - spec/plugins/campaign/lib
tests:
  mocha:
    files:
      rootDirectory: spec/plugins/campaign
      includes:
        - test/*.js
    assert: expect
    extraScripts:
      - /library.js